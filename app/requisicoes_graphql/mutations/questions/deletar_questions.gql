mutation DeleteQuestion {
    deleteQuestion(input: { id: "" }) {
        createdAt
        id
        options
        questionType
        required
        surveyId
        text
        updatedAt
    }
}
