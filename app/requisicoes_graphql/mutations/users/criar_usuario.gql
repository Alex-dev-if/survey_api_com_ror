mutation CreateUser {
    createUser(input: { username: "", password: "", role: "" }) {
        clientMutationId
        token
        user {
            id
            role
            username
        }
    }
}
